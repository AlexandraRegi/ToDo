import React from "react";
import { HeaderMain, HeaderTitle } from "../Header/Header.styles";

export default function Header() {
  return (
    <HeaderMain>
      <HeaderTitle>Things To Do</HeaderTitle>
    </HeaderMain>
  );
}
